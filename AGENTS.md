# AGENTS.md — 架构约束与开发约定

本文件是 MyApi 的架构事实来源。修改代码、拆分模块、写需求或评审前，先读这里。

## 1. 项目定位

MyApi 是面向 LLM 的 **API 网关 / 统一接入层**。对下游暴露 OpenAI 兼容协议，隐藏多个上游渠道的差异与密钥；负责鉴权、限速、限额、计费，以及模型路由、负载均衡、熔断与健康切换。

## 2. 技术栈

- Web 框架：gin
- 日志：`log/slog`
- 关系型数据库：PostgreSQL（GORM）
- 分布式缓存与状态：Redis（go-redis v8）
- 熔断：gobreaker
- 限流：自研 Redis 滑动窗口（当前桶 + 前一桶按过期比例估算）
- 分词：tiktoken-go/tokenizer

## 3. 模块划分与职责边界

一级功能模块固定为 7 个，详细设计见对应 Issue（#6–#15 后端模块、控制台模块）。

| 模块 | 解决的问题 | 核心归属 | 不负责 |
| --- | --- | --- | --- |
| M1 下游接入 | 下游如何用一个 OpenAI 兼容入口接入 | 对外 API 契约、请求上下文、协议转换 | 账户状态、路由决策、费用计算 |
| M2 下游管理 | 谁在调用、凭据与余额如何管理 | 用户、网关 Key、账户余额、资金流水 | 请求转发、渠道配置、费用计算 |
| M3 鉴权与配额 | 每次调用是否放行、限速与冻结 | 鉴权结果、配额窗口、预冻结额度 | 用户资料维护、渠道选择、最终结算 |
| M4 上游渠道管理 | 上游渠道与模型映射如何维护 | 渠道、渠道模型、上游密钥、单价 | 路由选择、熔断判定、对外暴露 |
| M5 模型路由 | 请求该走哪个健康渠道 | 路由决策、健康状态、熔断状态、粘性映射 | 渠道配置、扣费、协议解析 |
| M6 用量统计与计费 | 用量与费用如何计量与呈现 | 用量明细、日汇总、结算结果 | 请求转发、路由、定价维护 |
| M7 管理控制台 | 运营方如何可视化管理与查看 | 管理视图、文档阅读 | 绕过模块读写数据、参与转发与计费 |

### 模块协作约束（硬规则）

- 模块之间只能通过明确的命令、查询结果或事件传递数据。
- 一个模块可以读取其他模块公开的结果，**不能直接修改其他模块拥有的状态**。
- 只有下游接入层直接面向下游；只有上游渠道管理持有上游密钥并作为对上游发起调用的凭据来源。
- 上游密钥明文只允许在转发瞬间由渠道管理解密提供，其他模块不得持有。
- 账户余额与资金流水只允许下游管理修改；鉴权与配额、计费通过命令请求冻结、扣减与解冻。
- 路由决策只允许模型路由模块产生；接入层与配额模块不得自行选择渠道。

### 关键事件

事件只表达「已经发生的事实」，不表达命令，命名格式 `[领域].[事实]`：

| 事件 | 产生 | 消费 | 事实 |
| --- | --- | --- | --- |
| `gateway.request_received` | 接入 | 配额 | 收到一次下游调用 |
| `auth.key_validated` | 下游管理 | 接入 / 配额 | 网关 Key 校验通过 |
| `quota.reservation_granted` | 配额 | 接入 / 路由 | 已通过限速并预冻结 |
| `quota.limit_exceeded` | 配额 | 接入 | 命中限速或余额不足 |
| `routing.channel_selected` | 路由 | 接入 / 渠道 | 已选定健康渠道 |
| `channel.health_changed` | 路由 | 控制台 | 渠道健康状态变化 |
| `channel.circuit_opened` | 路由 | 渠道 / 控制台 | 渠道被熔断 |
| `upstream.request_completed` | 接入 | 计费 | 上游调用结束并产生 usage |
| `billing.settlement_completed` | 计费 | 下游管理 / 控制台 | 完成费用结算 |
| `account.balance_changed` | 下游管理 | 配额 / 计费 / 控制台 | 账户余额变化 |
| `channel.config_changed` | 渠道 | 路由 / 控制台 | 渠道或映射变化 |
| `pricing.updated` | 渠道 | 计费 / 控制台 | 单价更新 |

## 4. 配置与状态位置

| 位置 | 归属 | 保存内容 | 是否进入版本控制 |
| --- | --- | --- | --- |
| PostgreSQL | 平台（服务端） | 用户、网关 Key、渠道、渠道模型、单价、限流规则、用量明细、日汇总、余额与流水 | 不进入；唯一事实源 |
| Redis | 运行时 | 限流窗口、并发占用、预冻结额度、余额缓存、渠道健康与熔断、粘性路由、用量增量 | 不进入；可重建的临时状态 |
| `configs/config.yaml` | 部署 | 端口、数据库/Redis 连接、路由/计费/限流/统计参数、日志 | 模板可版本化 |
| 环境变量 `MYAPI_*` | 部署 | 覆盖配置；`MYAPI_APIKEY_ENC_KEY` 为上游密钥加密主密钥 | 密钥不进入版本控制 |
| `data/` | 运行时 | 自动生成的上游密钥加密密钥文件 | 不进入（已 gitignore） |

## 5. 数据模型（表）

- `channels`：上游渠道（base_url、加密 api_key、状态、权重、优先级、本地余额）
- `channel_models`：对外模型名 → 上游真实模型名映射
- `model_pricing`：渠道 × 模型 的输入/输出/cached 单价（USD / 1M tokens）
- `users` / `client_api_keys`：下游用户与网关 Key（Key 只存哈希）
- `user_balances` / `balance_transactions`：余额与资金流水
- `rate_limit_rules`：按 global/user/api_key/model/channel 维度的限流规则
- `usage_logs`：逐请求用量明细
- `user_daily_stats`：用户 × 日期 的用量与费用汇总

表结构以 GORM 模型 `internal/model/*.go` 为准；生产环境手动建表脚本见 `migrations/0001_init.sql`（开发环境可用 `AutoMigrate`）。

## 6. 安全不变量

- 网关 Key 只存哈希（`key_hash`），明文仅在创建/重置响应出现一次。
- 上游 api_key 必须加密存储（`enc:v1:` 前缀，AES-GCM），解密入口唯一。
- 密钥明文不得写入日志、磁盘或响应；用后 `secret.Zero` 清零。
- 管理端 `/admin` 不做鉴权，必须部署在内网，禁止直接暴露公网。

## 7. 构建、测试与运行

```bash
go build ./...      # 编译
go vet ./...        # 静态检查
go test ./...       # 单元测试
go run ./cmd/server -config configs/config.yaml   # 本地运行
docker compose up --build                          # 容器运行
```

提交前至少通过 `go build ./...`、`go vet ./...`、`go test ./...`。

## 8. 代码与协作约定

- 不要添加无关注释；只在必要处说明「为什么」。
- 新增/修改模块时，同步更新对应 Issue 的模块设计与 `docs/` 接口文档。
- 提交 PR 使用 `.github/pull_request_template.md`，并关联 Issue（`Closes #`）。
- 下游接口保持 OpenAI 兼容；管理端接口固定 `{code,message,data}` 风格，HTTP 恒 200。
- 金额字段对外用字符串传输，避免浮点精度丢失。
