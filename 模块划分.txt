1. API模块，使用 OpenAI Chat Completions 协议，解析下游请求的网关 Key。

2. 下游管理模块，管理下游使用的网关 Key，校验网关 Key的有效。

3. 限速与配额控制模块，对下游网关 Key 进行限速和限额。

4. 模型路由模块，根据下游请求中的模型名，匹配所有可用且健康的上游渠道，在多个渠道之间执行负载均衡。对渠道进行健康检查，熔断与不健康节点的检查。

5. 上游渠道管理模块，管理多个相互独立的上游 LLM 渠道，支持动态增删改。

6. 用量统计与费用计量模块。